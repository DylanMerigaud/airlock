# Synthetic inputs

Every synthetic element that appears in the demo, the video or the README is listed here. Anything
not on this list is real and named in `assets/real/SOURCE.md`.

## assets/synthetic/nimbus-test-clip.mp4 (labelled "synthetic test asset" on screen)

- What: an 8 s, 1920x1080, 24 fps clip of an unbranded white can of sparkling water on an oak table.
- Generated 2026-08-28 with Veo on Vertex AI, model `veo-3.1-generate-001` (GA), one sample,
  `person_generation=dont_allow`, no audio, 126 s of generation, no responsible-AI filtering
  (`scripts/generate_synthetic_clip.py`). Raw output sha256: a6667d0f35851828cc9dad4813394a3df4f99bed7fef4fe97b394616ae6c5f6f
- Overlaid with ffmpeg drawtext (`scripts/make_synthetic_asset.sh`): the fictional wordmark "Nimbus",
  the tagline "Clear as morning.", and from 3 s the claim "Recommended by 9 out of 10 sommeliers."
  The claim is there on purpose: it is an expert endorsement with nothing behind it, the shape
  16 CFR 255.3 regulates, and the claim gate must block it.
- Signed with c2patool 0.27.16 (c2pa-rs 0.90.16) and a self-issued ES256 test certificate
  ("Airlock self-issued test signer", generated by the same script, never committed). The manifest
  records `c2pa.created` with digitalSourceType trainedAlgorithmicMedia (Veo 3.1) and
  `c2pa.edited` (ffmpeg overlay). The certificate is in `trust/trust-anchors.pem` so the
  provenance gate treats it as the studio's own signer.
- Signed file sha256: 11ce2b0e439c87b6819adefc5857d431c076e23405192e5c6190489500f3047d
- The brand "Nimbus", its charter (`charter.yaml`) and its registry entry are fictional.

## Nothing else

The Crest commercial, the FTC text, the ASA rulings, the Grafana data and every number on screen
are real and named at their source.
