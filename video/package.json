{
  "name": "airlock-video",
  "private": true,
  "type": "module",
  "description": "Records the live Airlock reviewer console as a take, and logs the cue times the narration is laid on.",
  "scripts": {
    "prep": "node record.mjs --prep",
    "record": "node record.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
